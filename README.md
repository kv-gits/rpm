# RPM - Rust Password Manager

Безопасный менеджер паролей на Rust с поддержкой TUI, системного трея и расширений для браузеров.

## Особенности

- 🔐 **Мощное шифрование**: AES-256-GCM, Argon2id, ChaCha20Poly1305
- 🖥️ **TUI интерфейс**: Удобный терминальный интерфейс на базе ratatui
- 📱 **Системный трей**: Быстрый доступ из системного трея
- 🌐 **Расширения браузера**: Поддержка Chrome и Firefox через локальный сервер
- 🔒 **Безопасность**: Все пароли хранятся в зашифрованном виде
- 💾 **Локальное хранение**: SQLite база данных на вашем компьютере

## Архитектура

```
rpm/
├── src/
│   ├── main.rs          # Точка входа
│   ├── config.rs        # Конфигурация приложения
│   ├── errors.rs        # Типы ошибок
│   ├── models.rs        # Модели данных
│   ├── crypto/          # Модуль криптографии
│   │   ├── mod.rs
│   │   └── key_derivation.rs
│   ├── db/              # Работа с базой данных
│   │   └── mod.rs
│   ├── tui/             # Терминальный интерфейс
│   │   └── mod.rs
│   ├── tray/            # Системный трей
│   │   └── mod.rs
│   └── server/          # HTTP сервер для расширений
│       └── mod.rs
├── Cargo.toml
├── .cursorrules         # Конвенции для разработки
└── README.md
```

## Криптография

### Используемые алгоритмы

- **AES-256-GCM**: Симметричное шифрование паролей
- **Argon2id**: Хеширование мастер-паролей
- **ChaCha20Poly1305**: Альтернативный алгоритм шифрования
- **Ed25519**: Цифровые подписи
- **X25519**: Обмен ключами

### Безопасность

- Все пароли хранятся только в зашифрованном виде
- Мастер-пароль никогда не сохраняется
- Используется аутентифицированное шифрование
- Криптографически стойкие генераторы случайных чисел

## Установка

```bash
# Клонировать репозиторий
git clone <repository-url>
cd rpm

# Собрать проект
cargo build --release

# Запустить
cargo run
```

## Использование

### Первый запуск

При первом запуске приложение создаст:
- Конфигурационный файл в `~/.config/rpm/config.toml`
- Базу данных в `~/.local/share/rpm/rpm.db`

Вам будет предложено создать мастер-пароль.

### TUI

Запустите приложение и используйте:
- `↑/↓` - Навигация по списку паролей
- `q` - Выход
- Другие команды будут добавлены в будущих версиях

### HTTP API

Сервер запускается на `127.0.0.1:8765` по умолчанию.

Эндпоинты:
- `GET /health` - Проверка здоровья сервера
- `POST /api/auth` - Аутентификация
- `POST /api/passwords` - Создание пароля
- `GET /api/passwords` - Список паролей

## Расширения браузера

Расширения для Chrome и Firefox будут разработаны отдельно. Они будут подключаться к локальному серверу через HTTP API.

## Разработка

См. файл `.cursorrules` для конвенций разработки.

### Зависимости

- Rust 1.70+
- SQLite3
- Системные библиотеки для системного трея (зависят от платформы)

### Тестирование

```bash
cargo test
```

### Линтинг

```bash
cargo clippy
cargo fmt
```

## Лицензия

MIT OR Apache-2.0

## Статус проекта

🚧 **В разработке** - Это каркас приложения. Основная функциональность будет добавлена в будущих версиях.

## Безопасность

⚠️ **ВАЖНО**: Это проект в разработке. Не используйте для хранения реальных паролей до завершения аудита безопасности.

## Roadmap

- [x] Базовая структура проекта
- [x] Модуль криптографии
- [x] База данных
- [x] TUI интерфейс (базовый)
- [x] HTTP сервер (базовый)
- [ ] Полная реализация TUI
- [ ] Системный трей
- [ ] Расширения для браузеров
- [ ] Импорт/экспорт паролей
- [ ] Генератор паролей
- [ ] Аудит безопасности

